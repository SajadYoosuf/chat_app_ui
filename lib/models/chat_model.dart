// DATA MODELS
class ChatModel {
  final String name;
  final String lastMessage;
  final String time;
  final int unreadCount;

  ChatModel(this.name, this.lastMessage, this.time, this.unreadCount);
}
